# Public site URL (no trailing slash). Set in Vercel → Project → Environment Variables
# for correct Open Graph, sitemap, and canonical URLs. Falls back to VERCEL_URL on Vercel.
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com
